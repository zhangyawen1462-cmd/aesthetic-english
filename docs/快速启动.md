# 🚀 快速启动指南

## 立即测试权限系统

### 1️⃣ 启动开发服务器

```bash
npm run dev
```

### 2️⃣ 访问测试页面

在浏览器中打开：
```
http://localhost:3000/test-permissions
```

### 3️⃣ 使用开发者面板

1. 点击右下角黑色圆形按钮（眼睛图标）
2. 输入密码：`456852`
3. 选择会员类型：
   - **未登录** - 只能看 Sample
   - **quarterly** (季度会员) - Daily 全解锁，Cognitive 仅 Sample，Business 锁定
   - **yearly** (年度会员) - 所有课程 + AI 15次/期 + 笔记导出
   - **lifetime** (永久会员) - 所有功能无限制

### 4️⃣ 观察权限变化

切换会员类型后，页面会自动刷新，你会看到：
- ✅ 视频访问权限矩阵
- ✅ Gabby AI 配置变化
- ✅ 下载权限变化
- ✅ 会员权益对比

---

## 📁 核心文件

| 文件 | 说明 |
|------|------|
| `lib/permissions.ts` | 权限"宪法"（11KB） |
| `components/DevPanel.tsx` | 开发者面板 |
| `components/ContentGate.tsx` | 门禁组件 |
| `app/test-permissions/page.tsx` | 测试页面 |

---

## 🎯 测试场景

### 场景 1：季度会员看 Business 课程

1. 开发者面板选择 `quarterly`
2. 尝试访问 Business 课程
3. 应该看到：磨砂玻璃遮罩 + 升级提示

### 场景 2：年度会员使用 AI

1. 开发者面板选择 `yearly`
2. 打开 Gabby AI
3. 应该看到：15 次/期限制

### 场景 3：永久会员下载原片

1. 开发者面板选择 `lifetime`
2. 点击下载按钮
3. 应该看到：所有下载选项可用

---

## 📖 完整文档

- [会员体系指南](./会员体系指南.md) - 完整的系统说明
- [权限系统迁移指南](./权限系统迁移指南.md) - V1 到 V2 迁移
- [迁移完成报告](./迁移完成报告.md) - 迁移总结

---

## 💡 快速使用

### 在组件中检查权限

```typescript
import { checkVideoAccess, getGabbyConfig, canDownloadRawVideo } from '@/lib/permissions';

// 检查视频访问权限
const canWatch = checkVideoAccess(tier, 'business', false);

// 获取 AI 配置
const aiConfig = getGabbyConfig(tier);

// 检查下载权限
const canDownload = canDownloadRawVideo(tier);
```

### 使用门禁组件

```typescript
import ContentGate from '@/components/ContentGate';

<ContentGate 
  requiredTier="yearly" 
  currentTier={membershipType} 
  feature="Business 课程"
>
  <YourContent />
</ContentGate>
```

---

**开始测试吧！** 🎉







